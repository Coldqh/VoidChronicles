Void Chronicles v0.32.1 — Test Suite Hotfix

Исправлено:
- interfaceV32.test.ts проверяет реальную кнопку «Принять» и action id accept-operation,
  а не несуществующий текст «Принять операцию».
- Тяжёлому ecology-cycle тесту задан локальный timeout 15 секунд.
- Production-код операций и экологии не менялся.
- Версия поднята до 0.32.1.

Причина:
Первое падение было ошибкой моего теста.
Второе — тяжёлый детерминированный тест превысил общий лимит 5 секунд
при полном параллельном прогоне, хотя сама логика дошла до выполнения.

Установка:
1. Распаковать ZIP поверх C:\VoidChronicles.
2. Выполнить node APPLY-v0.32.1.mjs
3. Выполнить npm run check
