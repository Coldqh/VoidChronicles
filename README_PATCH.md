# Void Chronicles v0.10 Simulation Kernel

This patch adds the independent world clock, deterministic event scheduler, player-knowledge layer, event-derived news/contracts, save schema v11 and store integration.

Apply once from the project root:

```powershell
node .\scripts\apply-v010-simulation-kernel.mjs
```
