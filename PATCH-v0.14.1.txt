Void Chronicles v0.14.1 — Historical War Link Hotfix

Исправлено:
- событие войны теперь имеет явный ID deep_event_war_<war.id>;
- связь войны с её записью в хронике больше не теряется из-за повторного хэширования;
- тест проверяет точную причинную связь, а не случайное совпадение последних символов;
- генерация войн, миграций, потерь и разрушений не изменяется;
- версия приложения и npm-пакета обновляется до 0.14.1.

Применение:
node .\scripts\apply-v0141-war-link-hotfix.mjs
